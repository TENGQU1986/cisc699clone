import React from "react";
import loading from "./images/loader.gif";

const Loading = () => {
  return (
    <div>
      <img src={loading} alt="" height={200} width={200} />
    </div>
  );
};

export default Loading;
